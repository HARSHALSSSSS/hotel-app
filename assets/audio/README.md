# Incoming Call Ringtone

Place a phone ringtone file named `ringtone.mp3` in this folder for the hotel support incoming call sound.

If no valid ringtone is found, the app will try fallback URLs. For best experience, use a short looping phone ring (~2-5 seconds) in MP3 format.
